services:
  mt5:
    build:
      context: .
    image: metatrader5-service:dev
    platform: linux/amd64
    restart: unless-stopped
    security_opt:
      - seccomp:unconfined
    ports:
      - "3000:3000"
      - "${HTTP_PORT:-8000}:${HTTP_PORT:-8000}"
    volumes:
      - .:/workspace/metatrader5-service
    environment:
      CUSTOM_USER: ${VNC_USER}
      PASSWORD: ${VNC_PASSWORD}
      PUID: "1000"
      PGID: "1000"
      HTTP_PORT: ${HTTP_PORT:-8000}
